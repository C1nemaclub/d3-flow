import { TextField } from '@mui/material';

const AdaptableField = () => {
  return (
    <TextField
      label='Flexible'
      size='small'
      fullWidth
      color='warning'
      focused
    />
  );
};

export default AdaptableField;
